# 第1课完成：神经元、激活函数、梯度下降

用户确认已理解：
- 一个神经元 = 加权求和 + 偏置，和线性回归本质相同
- 多层纯线性变换复合后仍是线性，因此需要激活函数引入非线性
- 学习 = 前向传播算 loss → 反向传播算梯度 → 梯度下降更新参数
- 学习率控制参数更新步长

状态：已建立深度学习的最基本心智模型。可以进入多层网络和反向传播的完整推导。

Evidence: 用户自学完成第1课（含自测题），表示"已经学习完毕"。
Implications: 第2课可以直接讲 MLP + 完整的反向传播推导 + PyTorch 实操。
